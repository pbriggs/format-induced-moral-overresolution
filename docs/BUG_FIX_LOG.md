# Bug Fix Log

Use this log for fixes after `run_manifest_v1.md` is frozen or during engineering shakedowns.

| Date | Run context | Files changed | Problem | Fix | Scientific-facing? | Manifest update needed? |
|---|---|---|---|---|---|---|
| 2026-06-16 | pre-freeze | parser/protocol/docs | Added engineering shakedown milestones and deterministic extracted-JSON status before real provider collection. | Added `1`, `10`, `50`; added `valid_extracted_json`; documented process. | Yes | Included before v1 freeze |
| 2026-06-16 | `1` engineering shakedown | `src/prompts/prompt_templates.py`, `tests/test_core_pipeline.py`, `run_manifest_v1.md` | First real `gpt-5.5` distribution call returned a bare label-probability object instead of the frozen schema wrapper, causing `missing_required_field`. | Added explicit required top-level field names and JSON shape examples to prompt templates; added prompt contract regression test. | Yes | Updated prompt template hash before further shakedowns |
| 2026-06-16 | `10` engineering shakedown | `src/production/execute_milestone.py`, `tests/test_core_pipeline.py` | Per-shard execution summary counted `valid_extracted_json` as invalid even though milestone reports and the protocol include it in primary-valid outputs. | Reused the primary exclusion rule for the executor invalid-output counter and added a regression test. | No; reporting consistency fix | No prompt/schema hash change |
| 2026-06-16 | `10` engineering shakedown | `src/production/providers.py`, `tests/test_core_pipeline.py`, `run_manifest_v1.md` | Both `gemini-3.5-flash` calls stopped with `finishReason=MAX_TOKENS` and truncated JSON. | Google adapter now requests `application/json` responses and sets Gemini 3-family `thinkingConfig.thinkingLevel=low` for compact classification outputs. | Yes; provider generation config | Manifest updated before further shakedowns |
| 2026-06-16 | `10` engineering shakedown with Qwen/DeepSeek roster | `src/production/providers.py`, `src/production/reporting.py`, `tests/test_core_pipeline.py`, `run_manifest_v1.md` | Report generation crashed when an invalid distribution row lacked probability fields; one `gpt-5.5` distribution response truncated under the Responses output budget. | Distribution-gap reporting now skips incomplete distribution rows; OpenAI GPT-5/o-series calls use `reasoning.effort=minimal` and `max_output_tokens=1024`. | Yes; provider generation config and reporting robustness | Manifest updated before further shakedowns |
| 2026-06-16 | `10` engineering shakedown with Qwen/DeepSeek roster | `src/production/providers.py`, `tests/test_core_pipeline.py`, `run_manifest_v1.md` | `gpt-5.5` rejected `reasoning.effort=minimal` with HTTP 400; supported values include `none`, `low`, `medium`, `high`, and `xhigh`. | Changed GPT-5/o-series compact JSON calls to `reasoning.effort=none`. | Yes; provider generation config | Manifest updated before further shakedowns |
| 2026-06-16 | `50` engineering shakedown | `src/production/providers.py`, `src/production/reporting.py`, `tests/test_core_pipeline.py`, `run_manifest_v1.md` | One Gemini distribution call truncated with `finishReason=MAX_TOKENS`; report decision said `continue` despite `validity_gate.proceed=false`. | Increased Gemini adapter `maxOutputTokens` to 1024 and made milestone decisions honor failed validity gates. | Yes; provider generation config and reporting decision | Manifest updated before further shakedowns |
| 2026-06-17 | isolated xAI/Grok replacement shakedown | `src/production/config.py`, `src/production/providers.py`, `tests/test_core_pipeline.py` | Explicit `STUDY_MODEL_PROVIDER_MAP` entries for unknown model slugs still evaluated the inference fallback, so OpenRouter-hosted xAI model IDs failed configuration before provider execution; direct xAI routing was not yet configured. | Made explicit provider-map entries take precedence without evaluating inference; added a direct xAI Responses API provider for `grok-4.3`; added regression coverage. | Yes; model routing/configuration | Document in protocol amendment before replacing Gemini in production roster |
| 2026-06-17 | amended 3k completion after Gemini-to-Grok switch | `src/production/progress_status.py`, `tests/test_core_pipeline.py` | Progress reporting counted superseded pre-amendment Gemini planned/error rows in the same run database, showing `3600` planned calls and causing the unattended runner to keep looping after the amended target had no provider-executable calls left. | Scoped progress counts to the current `target_api_call_ids_<milestone>.jsonl` when available; added regression coverage. | No output/prompt change; operational reporting fix for amended roster | No; amendment manifest already documents the provider substitution |
| 2026-06-17 | 3k paraphrase-audit prework | `src/production/materialize_paraphrases.py`, `materialize_paraphrases.bat`, `src/production/run_milestone.py`, `src/production/providers.py`, `docs/STUDY_PROCESS.md` | The 500 paraphrase-audit calls were correctly prework-blocked, but no command existed to materialize paraphrase pairs and unblock them; replanning would also overwrite materialized paraphrase prompts; one helper response used literal line breaks inside the JSON string. | Added a paraphrase materialization command, mock paraphrase support, planner preservation for already-materialized paraphrase requests, and narrow coercion for helper outputs with literal newlines in `paraphrased_situation`. | Yes; pre-planned paraphrase-audit execution plumbing | No model/prompt schema change; document generated paraphrase helper model in milestone artifacts |
| 2026-06-17 | 6k milestone review | `src/production/progress_status.py`, `src/production/reporting.py`, `src/pilot/pilot_diagnostics.py`, `src/protocol/call_milestones.py`, `tests/test_core_pipeline.py` | Cumulative progress counted only rows introduced at the current milestone when target IDs spanned prior milestone rows; 6k written milestone criteria also required contested agreement surplus to exceed high-consensus surplus, but that observed field was not exposed in reports/decisions. | Made target-scoped progress cumulative across milestone prefixes; added high-consensus agreement-surplus reporting and a contested-surplus threshold for 6k+ decisions; added regression coverage. | No collected-output change; milestone decision/reporting alignment fix | Include in post-6k release notes before 13k |
| 2026-06-18 | 13k production execution | `src/production/providers.py`, `tests/test_core_pipeline.py` | A transient chunked-transfer read failure (`http.client.IncompleteRead`) could escape the provider retry wrapper and crash the executor instead of being treated as a retryable transport error. | Added `http.client.HTTPException` to retryable transport exceptions in `post_json_with_retry`; added regression coverage for an incomplete chunked read followed by a successful retry. | No prompt/model/output change; transport robustness fix | Include in next milestone/release notes |
| 2026-06-18 | 13k production review | `src/production/progress_status.py`, `src/production/reporting.py`, `src/production/run_milestone.py`, `execute_remaining_milestone_run.bat`, `tests/test_core_pipeline.py` | The unattended runner could report all executable shards passed while paraphrase prework remained; milestone reporting/planning also treated a non-error HTTP 200 empty model response as incomplete rather than as a completed invalid output. | Added a distinct prework-blocked progress status/message; counted non-error attempted rows as completed for milestone-completion gating and cumulative replanning while leaving invalid-output handling to validity metrics; added regression coverage. | No prompt/model/output change; operational/reporting robustness fix | Include in post-13k release notes before 25k |
