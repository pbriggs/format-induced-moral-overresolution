# Run Manifest v1

This file is the pre-3k freeze artifact to upload to the editable OSF project and to the public/review repository before provider collection begins. Values marked `[fill before provider execution]` must be replaced with the final frozen values before any real API calls.

## 1. Exact Model Roster, Provider/API Route, Model IDs, Version Strings

The run requires exactly five frozen model IDs in `STUDY_MODEL_IDS`, in slot order:

| Slot | Provider / route | Planned family role | Frozen model ID | Version / snapshot if available |
|---:|---|---|---|---|
| 1 | OpenAI first-party Responses API | GPT-family frontier proprietary | `gpt-5.5` | OpenAI model docs, verified 2026-06-16 |
| 2 | Google first-party Gemini API | Gemini-family frontier proprietary | `gemini-3.5-flash` | Google Gemini API model docs, stable model, verified 2026-06-16 |
| 3 | Anthropic first-party Messages API | Claude-family frontier proprietary | `claude-sonnet-4-6` | Anthropic model docs, verified 2026-06-16 |
| 4 | OpenRouter-compatible `LLAMA_*` route | stronger open/open-weight-derived model, preferably Qwen/DeepSeek/Mistral or comparable | Candidate: `qwen/qwen3.7-max`; freeze after shakedown | OpenRouter model list, verified 2026-06-16 |
| 5 | Same family as slot 4 | smaller/weaker same-family model for capability contrast | Candidate: `qwen/qwen3.7-plus`; freeze after shakedown | OpenRouter model list, verified 2026-06-16 |

Environment variables used by the runner:

- Candidate shakedown value: `STUDY_MODEL_IDS=gpt-5.5,gemini-3.5-flash,claude-sonnet-4-6,qwen/qwen3.7-max,qwen/qwen3.7-plus`
- `OPENAI_API_KEY`, `OPENAI_MODEL`, optional `OPENAI_BASE_URL`
- `GOOGLE_API_KEY`, `GOOGLE_MODEL`, optional `GOOGLE_GENAI_BASE_URL`
- `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL`, optional `ANTHROPIC_BASE_URL`
- `LLAMA_API_KEY`, `LLAMA_MODEL`, optional `LLAMA_BASE_URL` for the OpenRouter-compatible route. The environment-variable names are historical; Llama-family models are not required.
- optional `STUDY_MODEL_PROVIDER_MAP` if any model ID cannot be inferred

## 2. API Parameters And Structured-Output Settings

- Temperature: `0.0` for deterministic core modes.
- Top-p: provider default unless explicitly recorded in `api_calls_raw.top_p_or_null`.
- Max tokens: provider adapter default `512` unless frozen differently before execution. The OpenAI Responses adapter uses `max_output_tokens=1024` for GPT-5/o-series routes because OpenAI Responses output budgets include reasoning tokens as well as visible output.
- OpenAI reasoning control: for GPT-5/o-series routes, the adapter sets `reasoning.effort=minimal` for these compact JSON classification calls.
- Structured output: prompt-level compact JSON-only instruction; `structured_output_mode` is stored per call. The Google Gemini adapter additionally requests `responseMimeType=application/json`.
- Gemini thinking control: for Gemini 3-family models, the Google adapter sets `thinkingConfig.thinkingLevel=low` to reserve the 512-token output budget for the required compact JSON answer.
- Conversations: each prompt mode is executed as an independent stateless request.
- Retries: HTTP retry and circuit-breaker policy in `src/production/providers.py` and `src/production/failure_policy.py`.
- Deterministic JSON extraction: strict JSON is preferred; if a response contains extra text plus exactly one balanced JSON object, that object is extracted, validated, raw text is preserved, and status is `valid_extracted_json`.

## 3. Prompt Template Files And Hashes

Prompt templates are generated by:

- `src/prompts/prompt_templates.py`
- prompt hash function: `src/prompts/prompt_hashing.py`

Per-call rendered prompt hashes are written to:

- `planned_api_calls.prompt_hash`
- `api_calls_raw.prompt_hash`
- uploadable `call_ledger_<milestone>.csv/jsonl`

Template-source hash to freeze before provider execution:

- `src/prompts/prompt_templates.py`: `7F673DD3CA14D3780B53C05D16B67DF2AF98C7C750FC97256938A9976F32599B`
- `src/prompts/schemas.py`: `58A3C91E42CB8264253F1C705A6C6D2162405FC6167E61C9EC28F5EEFEF02619`

## 4. JSON Schemas / Response Fields

Response validation is implemented in:

- `src/parsing/validate_json.py`
- `src/parsing/validity_status.py`
- `src/protocol/prompt_modes.py`

Primary response fields:

- Distribution: `label_probabilities`, `most_likely_label`
- Descriptive verdict: `chosen_label`, `estimated_source_community_agreement`
- Sampling: `chosen_label`
- Normative certainty, when enabled: `chosen_label`, `moral_certainty`
- Recognition audit: `recognition_status`, `confidence`

## 5. Random Seed And Stratified Item-Selection Method

- Default milestone subset seed: `20260615`
- Default splits: `train,dev,test`
- Source distribution: `dirichlet_alpha_0.5_scruples_anecdotes_v1`
- Stratification bins:
  - high consensus: top-label support `>= 0.80`
  - moderate consensus: top-label support `>= 0.65 and < 0.80`
  - low consensus: top-label support `>= 0.50 and < 0.65`
  - diffuse: top-label support `< 0.50`
- Selection code:
  - `src/production/run_milestone.py`
  - `src/protocol/call_milestones.py`

## 6. Exact 3k Planned Call List

Before the 3k pilot, the following engineering shakedowns may be run and are excluded from study analyses:

- `1`: one real provider call to verify credentials, routing, parsing, and ledger writes.
- `10`: all five model slots across distribution and descriptive verdict for one item.
- `50`: small schema, latency, and outlier screen.

The exact 3k planned call list is generated by:

```powershell
.\start_milestone_run.bat 3k
```

Expected output:

- `runs/<RUN_ID>/pending_api_calls_3k.jsonl`
- `runs/<RUN_ID>/selected_items_3k.jsonl`
- `runs/<RUN_ID>/call_ledger_3k.csv`
- `runs/<RUN_ID>/call_ledger_3k.jsonl`
- `runs/<RUN_ID>/milestone_report_3k.md`
- `runs/<RUN_ID>/milestone_report_3k.json`

Freeze fields to fill before provider execution:

- `RUN_ID`: `production_milestones_cumulative_v1`
- `pending_api_calls_3k.jsonl` sha256: [fill after planning]
- `selected_items_3k.jsonl` sha256: [fill after planning]
- planned calls: `3000`
- provider-executable pending calls: [fill after planning]

## 7. Label Order / Counterbalancing Rule

For every item x model x prompt mode x sample, label order is deterministically shuffled using:

```text
stable_hash([seed, item_id, model_id, prompt_mode, sample_id])
```

The assignment is stored in:

- `prompt_assignments`
- `planned_api_calls.request_json`
- uploadable `call_ledger_<milestone>.csv/jsonl`

## 8. Retry And One-Repair Rule

Provider retry behavior:

- Retry statuses: `408`, `409`, `425`, `429`, `500`, `502`, `503`, `504`
- Honors `Retry-After` when available.
- Uses bounded exponential backoff plus jitter.
- Circuit breaker aborts on repeated retryable, 5xx, or rate-limit failures.

Repair policy freeze:

- Primary schema parsing currently stores strict-valid or invalid status.
- Any future one-repair prompt must be frozen before confirmatory collection and recorded by `repair_attempted`, `repair_successful`, and repair prompt hash fields.
- If repair is not implemented for a milestone, report it as `repair_attempted = 0`.

## 9. Exclusion Rules

Primary exclusions are defined in:

- `src/protocol/exclusion_rules.py`
- `src/parsing/validity_status.py`

Excluded from primary analyses:

- invalid after allowed repair
- missing required fields
- off-schema label outputs that cannot be unambiguously mapped
- probability outputs outside accepted range
- probability sums outside normalization tolerance
- refusals or safety-filter outputs
- API errors with no valid response
- empty responses

Exclusion information is stored in:

- `exclusion_log`
- uploadable `call_ledger_<milestone>.csv/jsonl`
- `milestone_report_<milestone>.md/json`

## 10. Pilot / Exploratory / Confirmatory Status

Frozen staging interpretation:

- `3k`: exploratory engineering pilot
- `6k`: exploratory pilot / first directional signal check
- `13k`: diagnostic pre-confirmatory run
- `25k`, `35k`, `50k`: confirmatory-capable milestones if the manifest and staged monitoring rules are frozen before collection

The code records this per planned/API call as:

- `is_pilot`
- `is_confirmatory`
- `call_type`
- `milestone`

Uploadable ledger and report files are written after planning/execution for OSF or GitHub release attachment.
