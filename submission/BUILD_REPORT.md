# Build report

Generated from latest uploaded `nmi_moral_overresolution_draft_50k_v5.md`.

Build results:

- `latex/main.pdf`: compiled successfully with `latexmk -xelatex`.
- `latex/supplementary_information.pdf`: compiled successfully with `latexmk -xelatex`.
- Render check completed with the PDF skill render workflow.
- Main manuscript tables were placed in a landscape block to avoid clipping/overlap.
- Supplementary Information tables were placed in landscape blocks where needed to avoid clipped hashes/paths.

Notes:

- This is a simple article-style LaTeX build for local review and packaging, not a final Nature/Springer production template.
- Figure and release-safe source-data directories are placeholders in this sandbox package; copy the actual repo figure PDFs/SVGs and source-data CSVs before final submission packaging.
- Restricted raw anecdotes, rendered prompts containing anecdote text, raw model/provider responses, full ledgers and full run stores should not be placed in this package for unrestricted sharing.
